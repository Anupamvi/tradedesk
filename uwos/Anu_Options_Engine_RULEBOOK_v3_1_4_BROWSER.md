# Anu Options Engine — BROWSER RULEBOOK ADDENDUM (Audited FIRE + SHIELD No-GEX Payload)

**Canonical upload filename:** `Anu_Options_Engine_RULEBOOK_v3_1_4_BROWSER.md`  
**Effective logic version:** 3.2.9-r7.2-exec-shortlist  
**Date:** 2026-04-26
**Revision:** r7.2 execution-shortlist user-facing fix

This browser addendum keeps the canonical project slot while preserving no-GEX automation.

## Browser policy

Browser context may be used for:

- page text already attached to the chat
- manual sanity checks
- human narrative context when explicitly requested

Browser context may **not** be used for automated GEX reads, inferred gamma walls, inferred flip levels, or any browser-only suppression or promotion of the base table.

## Base table protection

The browser layer may not suppress the primary inline table.

Browser scans inherit the same:

- base table contract
- EV/ML-first ranking
- Health Gate adapter
- no-fabricated-leg rule
- no-GEX policy
- FIRE debit support
- file-native SHIELD support
- iron-condor support only from file-native paired SHIELD rows

## Browser limits on SHIELD and condors

Browser context may not create a SHIELD structural anchor on its own.

A SHIELD anchor must come from file-native flow, hot-chain, and OI evidence. Browser text may be used only as a manual note when the user explicitly asks.


## v3.2.4 browser inheritance additions

Browser scans inherit scan-date OI by default plus explicit bounded next-day OI overlay only when requested, broker-native Health Gate execution label, and `Size != None` publication guard. Browser context still may not create a GEX, SHIELD, or condor anchor.

## Executable sync inheritance

Browser enrichment may not compensate for a stale root Python file. If executable sync fails, browser mode must block trade publication until all five canonical files are replaced from one bundle.


## v3.2.6 browser inheritance additions

Browser context still may not create a SHIELD anchor, GEX gate, or automated promotion. Browser scans inherit the family-flex translation pass only when the source is file-native and the translated structure is built from actual hot-chain legs. Browser-only context may support manual narrative notes but may not create a family-flex seed.

Browser scans also inherit the high-premium catalyst-watch requirement: earnings-blocked high-premium names must be surfaced as watch-only rather than silently dropped.


## v3.2.7 browser inheritance

Browser/Atlas context remains non-operative for automated ranking, gating, sizing, GEX, SHIELD anchors, and condor creation. Browser mode inherits full-source streaming, Top-Symbol Gap reporting, liquidity-tier routing, ETF/index lane separation, blocked-positive-EV output, alternates output, low-POP labels, and hard executable-sync blocking.


## r6 browser inheritance

Browser/Atlas context remains non-operative. It may not satisfy Mixed-Flow Rescue, create live quote proof unless an explicit broker/live-quote artifact is attached, override EV/ML ranking, or change OI handle policy. Browser mode inherits exact-date OI default, explicit-only next-day OI overlay, raw bot full-source schema inference, and ETF/index lane WATCH labeling.


## r7 browser clarification

Browser/Atlas remains non-operative for automated ranking, gating, sizing, GEX, SHIELD anchors, condor creation, and Mixed-Flow Rescue. Browser context may help a human read a broker page, but live quote validation requires an explicit attached broker/live-quote artifact or manually supplied quote fields. Browser absence must not suppress the EOD trade-idea tables.


## r7.2 execution-shortlist fix

Browser/Atlas context remains non-operative for automated ranking, gating, sizing, GEX, SHIELD anchors, condor creation, Mixed-Flow Rescue, and Execution Shortlist approval. Browser context may help a human read a broker page, but a row is not live-approved unless explicit live quote fields/artifacts are supplied.

The browser layer inherits the new user-facing rule: do not present the EV/ML primary table, high-conviction lane, alternates, ETF lane, or diagnostics as executable trade recommendations. User-facing recommendations must come from `Execution Shortlist / Preferred Trade Ideas` only.

Browser absence cannot suppress EOD ideas, but browser presence cannot promote a row into Execution Shortlist.
