[project]
name = "compoundcore"
version = "1.0.0"
requires-python = ">=3.9"
description = "Compound Core long-term index sleeve. No stock-picking, no options, no orders."

[tool.setuptools.packages.find]
include = ["compoundcore*"]
