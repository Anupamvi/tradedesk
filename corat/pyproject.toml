[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "corat"
version = "0.3.0"
description = "Evidence-first ORATS-backed stock and options swing research pipeline"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
corat = "corat.cli:main"

[tool.setuptools]
packages = ["corat"]
